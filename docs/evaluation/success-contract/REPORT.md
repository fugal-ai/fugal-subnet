# Offline success-contract mechanism report — evaluation in progress

The complete 10,000-question comparison is still running. This file is a status
placeholder, not the acceptance report. The final report will include separate
512- and 2048-token fits, held-out calibration and routing metrics, paired
baseline uncertainty, measured embedding performance, and reproducible exports.

The initial 2048-token attempt exceeded host memory and was discarded. The rerun
uses batches of two and saves progress every 200 questions. See
[execution notes](RUN_NOTES.md). No outcome is claimed before evaluation finishes.
