# Sole maintainer owns every path, including the consensus-critical and
# security-sensitive surfaces (fugal_subnet/graders.py, neurons/,
# .github/workflows/). Add per-path rules here if that ever changes.
* @jtdoherty
